import {StyleSheet} from 'react-native';

export const theme = StyleSheet.create({
  backgroundDefault: {
    flex: 1,
    backgroundColor: 'black',
  },
  textDefault: {
    color: 'white',
    fontSize: 20,
  },
});
